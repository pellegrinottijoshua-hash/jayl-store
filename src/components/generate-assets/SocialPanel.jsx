// SocialPanel — Batch 3 will implement full functionality
// For now: placeholder showing the section exists

export default function SocialPanel({
  productId, productName, productType, primaryColor, collection,
  allImages, onAssetSaved,
}) {
  return (
    <div className="p-6 text-center">
      <p className="text-gray-600 text-xs">📣 Social panel — coming in Batch 3</p>
    </div>
  )
}
